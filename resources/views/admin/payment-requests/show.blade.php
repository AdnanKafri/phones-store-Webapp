@extends('admin.layout')
@section('title', 'Payment Request Details')
@section('page-title', 'Payment Request')
@section('content')
<div class="page-header mb-4"><nav aria-label="breadcrumb"><ol class="breadcrumb"><li class="breadcrumb-item"><a href="{{ route('admin.dashboard') }}">Dashboard</a></li><li class="breadcrumb-item"><a href="{{ route('admin.payment-requests.index') }}">Payment Requests</a></li><li class="breadcrumb-item active">View</li></ol></nav><h2 class="page-title">Payment Request #{{ $paymentRequest->id }}</h2></div>
<div class="row"><div class="col-lg-8"><div class="card mb-4"><div class="card-header"><h5 class="mb-0">Request Details</h5></div><div class="card-body"><div class="mb-3"><p class="text-muted mb-1">User</p><h5>{{ $paymentRequest->user->name }}</h5><p class="text-muted">{{ $paymentRequest->user->email }}</p><a href="{{ route('admin.users.show', $paymentRequest->user) }}" class="btn btn-sm btn-outline-primary">View Profile</a></div><div class="row mb-3"><div class="col-md-6"><p class="text-muted mb-1">Payment Method</p><p><strong>{{ $paymentRequest->payment_method }}</strong></p></div><div class="col-md-6"><p class="text-muted mb-1">Amount</p><h4 class="text-success">${{ number_format($paymentRequest->amount, 2) }}</h4></div></div><div class="mb-3"><p class="text-muted mb-1">Reference Number</p><p>{{ $paymentRequest->reference_number ?? 'Not provided' }}</p></div>@if($paymentRequest->notes)<div class="mb-3"><p class="text-muted mb-1">User Notes</p><p>{{ $paymentRequest->notes }}</p></div>@endif<div class="mb-3"><p class="text-muted mb-1">Status</p><p>@if($paymentRequest->status === 'pending')<span class="badge bg-warning">Pending</span>@elseif($paymentRequest->status === 'approved')<span class="badge bg-success">Approved</span>@else<span class="badge bg-danger">Rejected</span>@endif</p></div>@if($paymentRequest->admin_notes)<div class="mb-3"><p class="text-muted mb-1">Admin Notes</p><p>{{ $paymentRequest->admin_notes }}</p></div>@endif<div class="mb-3"><p class="text-muted mb-1">Submitted</p><p>{{ $paymentRequest->created_at->format('F d, Y h:i A') }}</p></div></div></div><div class="card"><div class="card-header"><h5 class="mb-0">Approve or Reject</h5></div><div class="card-body">@if($paymentRequest->status === 'pending')<form action="{{ route('admin.payment-requests.update', $paymentRequest) }}" method="POST">@csrf @method('PUT')<div class="mb-3"><label for="status" class="form-label">Decision</label><select class="form-select" id="status" name="status" required><option value="pending">Pending</option><option value="approved">Approve (Add to Wallet)</option><option value="rejected">Reject</option></select></div><div class="mb-3"><label for="admin_notes" class="form-label">Admin Notes</label><textarea class="form-control" id="admin_notes" name="admin_notes" rows="3">{{ old('admin_notes', $paymentRequest->admin_notes) }}</textarea></div><div class="d-flex gap-2"><button type="submit" class="btn btn-primary"><i class="bi bi-check-circle me-2"></i>Update</button><a href="{{ route('admin.payment-requests.index') }}" class="btn btn-outline-secondary"><i class="bi bi-arrow-left me-2"></i>Back</a></div></form>@else<div class="alert alert-info">This request has already been {{ $paymentRequest->status }}.</div><a href="{{ route('admin.payment-requests.index') }}" class="btn btn-outline-secondary"><i class="bi bi-arrow-left me-2"></i>Back to List</a>@endif</div></div></div></div>
@endsection
